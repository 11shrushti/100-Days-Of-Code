#concatination 
# we need to put the space 
# to comment the text we use ctrl +/. and to undo it we use the ctrl +z
name=print("Hello " + input('Whats your name?'))
# to calculate the len of the string we need to use the len function
print(len( name))